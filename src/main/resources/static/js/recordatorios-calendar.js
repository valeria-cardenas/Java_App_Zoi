document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#calendarGrid');if(!grid)return;let shown=new Date();shown.setDate(1);const cards=[...document.querySelectorAll('.agenda-item')];const events=cards.map(el=>({date:el.dataset.date,title:el.dataset.title,pet:el.dataset.pet,time:el.dataset.time,type:el.dataset.type}));const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;function render(){grid.innerHTML='';document.querySelector('#monthTitle').textContent=shown.toLocaleDateString('es-CO',{month:'long',year:'numeric'});const first=new Date(shown.getFullYear(),shown.getMonth(),1),offset=(first.getDay()+6)%7,start=new Date(first);start.setDate(1-offset);for(let i=0;i<42;i++){const date=new Date(start);date.setDate(start.getDate()+i);const key=iso(date),cell=document.createElement('div');cell.className='calendar-day'+(date.getMonth()!==shown.getMonth()?' outside':'')+(key===iso(new Date())?' today':'');const number=document.createElement('span');number.className='day-number';number.textContent=date.getDate();cell.appendChild(number);const daily=events.filter(e=>e.date===key);daily.slice(0,3).forEach(e=>{const item=document.createElement('span');item.className=`calendar-event type-${e.type}`;item.innerHTML=`<b>${e.time}</b> ${e.title}`;item.title=`${e.pet}: ${e.title}`;cell.appendChild(item)});if(daily.length>3){const label=document.createElement('small');label.className='more-events';label.textContent=`+ ${daily.length-3} más`;cell.appendChild(label)}grid.appendChild(cell)}}document.querySelector('#prevMonth').onclick=()=>{shown.setMonth(shown.getMonth()-1);render()};document.querySelector('#nextMonth').onclick=()=>{shown.setMonth(shown.getMonth()+1);render()};document.querySelector('#todayButton').onclick=()=>{shown=new Date();shown.setDate(1);render()};document.querySelectorAll('.check-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const card=form.closest('.agenda-item');card.classList.add('is-completing');form.querySelector('.care-checkbox').classList.add('checked');setTimeout(()=>form.submit(),320)}));render()});
